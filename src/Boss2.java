public class Boss2 extends GameEntity {

    public Boss2(WeaponType weaponType, String name, int damage, int health) {
        super();
        this.setWeaponType(weaponType);
        this.setName(name);
        this.setDamage(damage);
        this.setHealth(health);


    }
}
